# Creator Hub — development / test dependencies
#
# Installs everything in requirements.txt plus the test toolchain.
# These are intentionally kept out of the production image.
#   pip install -r requirements-dev.txt

-r requirements.txt

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.3.4
httpx>=0.28.1
